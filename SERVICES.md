# Services

- netscout.service — Flask backend
- netscout-terminal.service — ttyd terminal
- netscout-kiosk.service — Chromium kiosk mode

Recommended:

```bash
systemctl status netscout
systemctl status netscout-terminal
systemctl status netscout-kiosk
```
